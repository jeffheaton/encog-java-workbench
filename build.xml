<project name="encog-workbench" default="dist" basedir=".">
	<description>
        Build the workbench JAR file for Encog.
    </description>
	<!-- set global properties for this build -->
	<property name="src" location="src" />
	<property name="build" location="bin" />
	<property name="dist" location="lib" />
	<property name="ext_jar" location="jar" />
	<property name="classpath" value="jar/encog-core.jar:jar/jfreechart-1.0.11.jar:jar/jcommon-1.0.14.jar" />


	<target name="init">
		<!-- Create the build directory structure used by compile -->
		<mkdir dir="${build}" />
	</target>

	<target name="compile" depends="init" description="compile the source ">
		<!-- Compile the java code from ${src} into ${build} -->
		<javac srcdir="${src}" source="1.5" target="1.5" destdir="${build}" classpath="${classpath}" />
	</target>

	<target name="dist" depends="compile" description="generate the distribution">
		<!-- Create the distribution directory -->
		<mkdir dir="${dist}" />

		<copy todir="${build}/resource">
			<fileset dir="src/resource"/>
		</copy>


		<!-- Put everything in ${build} into the encog-core.jar file -->
		<jar jarfile="${dist}/encog-workbench.jar" basedir="${build}" />
	</target>

	<target name="clean" description="clean up">
		<!-- Delete the ${build} and ${dist} directory trees -->
		<delete dir="${build}" />
		<delete dir="${dist}" />
	</target>
</project>